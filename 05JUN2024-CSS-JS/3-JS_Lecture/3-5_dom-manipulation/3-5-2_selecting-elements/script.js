// Get element by id
let divById = document.getElementById('myDiv');
console.log('Element by Id:', divById);

// Get elements by class name
let divsByClassName = document.getElementsByClassName('container');
console.log('Elements by Class Name:', divsByClassName);

// Using querySelector to select the first div with class 'container'
let divByQuerySelector = document.querySelector('.container');
console.log('Element by Query Selector:', divByQuerySelector);
