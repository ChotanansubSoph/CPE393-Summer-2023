// Checking if a student is eligible for a project review session
let hasCompletedProject = true;
if (hasCompletedProject) {
    console.log('✅ You are eligible for the project review session.');
    console.log('Please contact TA Chokun for scheduling.');
}